CREATE TABLE ExploreEntry{
    FORMATTED_ADDRESS varchar(255),
    FORMATTED_PHONE varchar(80),
    ID varchar(255),
    E_NAME varchar(255),
    LATITUDE REAL,
    LONGITUDE REAL,
    PRIMARY KEY(ID)
};